# 安全政策

[English](SECURITY.md)

## 支持范围

安全修复面向当前 `main` 和最新发布版本。旧提交与旧镜像可能不会单独回补。

## 报告安全问题

请将漏洞和疑似凭证泄露发送至 `iola1999@foxmail.com`，不要创建公开 Issue。

报告中可以包含：

- 受影响的提交、镜像标签或部署日期
- 部署方式，例如 Cloudflare Worker、Docker 或公共实例
- 可复现步骤与影响范围
- 已脱敏的浏览器控制台、Worker 或 Node 日志

请勿附带真实短信验证码、账号凭证 JSON、UU Token、房间 Token、完整设备 ID、私网地址或未经脱敏的请求与响应。

## 信任边界

UU Remote Web 在浏览器 `localStorage` 中保存登录状态，并通过当前部署转发带鉴权的 UU API 请求。部署的运营方在技术上可以观察经过该实例的请求，因此只能在自己控制或完全信任的实例中登录、输入验证码和导入账号凭证。

每个浏览器标签页会生成随机会话凭据，用于隔离 Node 进程或 Cloudflare Durable Object 中的信令状态。部署入口的访问权限仍需单独配置。公网自建实例建议放在 Cloudflare Access、带身份验证的反向代理或其他访问网关后面。

浏览器通过 WebRTC 与受控设备协商远控媒体和数据通道。默认自动路径会尝试局域网或 P2P 直连，条件不满足时可使用 UU 中转。Cloudflare Worker 负责静态页面、UU API 转发和信令网关，不承载 WebRTC 媒体流，也不会关闭直连能力。

## 公共实例

项目提供公共实例：[https://uurc.678234.xyz](https://uurc.678234.xyz)。它适合快速查看界面和验证基本流程。日常使用建议自行部署，特别是需要长期保存登录状态或频繁远控时。

使用任何共享实例前，请确认你信任其运营方。退出账号会清除当前浏览器保存的登录状态；服务端日志、网络代理和部署环境仍由实例运营方控制。

## 已知安全边界

- UU API 只允许项目白名单内的上游路径，仍会携带完成请求所需的账号信息。
- 随机会话凭据隔离不同标签页的信令状态，不提供用户身份验证。
- 远控诊断可能包含设备 ID、网络候选地址、房间状态和上游错误，分享前需要脱敏。
- Wisp 默认关闭。启用时仍受目标主机、端口和私网地址策略约束。
